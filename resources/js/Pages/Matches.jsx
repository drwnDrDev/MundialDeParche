import { Head } from '@inertiajs/react';

export default function Matches({ matchDays, groups, currentRound }) {
    return (
        <>
            <Head title="Partidos" />
            <div>Matches</div>
        </>
    );
}
